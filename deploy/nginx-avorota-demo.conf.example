# Пример для Nginx: домен → Next.js на localhost:3000
# Скопируйте в sites-available, поправьте пути к сертификатам (или сначала только HTTP).
# После правок: sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    listen [::]:80;
    server_name avorota.na4u.ru;

    # Редирект на HTTPS (раскомментируйте, когда выпустите сертификат):
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Пример HTTPS (после certbot или своих сертификатов):
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name avorota.na4u.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/avorota.na4u.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/avorota.na4u.ru/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
